#[derive(Debug)]
pub enum MusicTypes {
    DefaultMusicExtension,
    WavExtension,
    FlacExtension,
    MPThreeExtension,
    None,
}

#[derive(Debug)]
pub enum CoverArtTypes {
    PngExtension,
    JpegExtension,
    JpgExtension,
    None,
}
