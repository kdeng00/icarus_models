#[derive(Debug)]
pub enum MusicTypes {
    DefaultMusicExtension,
    WavExtension,
    FlacExtension,
    MPThreeExtension,
}

#[derive(Debug)]
pub enum CoverArtTypes {
    PngExtension,
    JpegExtension,
    JpgExtension,
}
